tmux attach || tmux new Ч дела€ так, вы сначала подключаетесь к уже существующей сессии, а если еЄ нет, то создаЄте новую. 
!!!!!!!!!!!!!!!!!(а можно добавить в .bash_aliases alias tmx='tmux -2 attach || tmux  )-2 new' !!!!!!!!!!!!

` + c Ч создать новое окно сессии
` + d Ч отключитьс€. (“очно так же вы отключитесь, если прервЄтс€ соединение.  ак подключитьс€ обратно и продолжить работу Ч см. выше.)

¬ одной сессии может быть сколько угодно окошек:
` + c Ч создать окошко;
` + 0...9 Ч перейти в такое-то окошко;
` + p Ч перейти в предыдущее окошко;
` + n Ч перейти в следующее окошко;
`` Ч перейти в предыдущее активное окошко (из которого вы переключились в текущее);
Ctrl + d Ч закрыть окошко (а можно просто набрать exit в терминале).

¬ одном окошке может быть много панелей:
` + | Ч разделить текущую панель на две, по вертикали;
` + _ Ч разделить текущую панель на две, по горизонтали (это кавычка, котора€ около Enter, а не Shift+2);
` + o Ч переходить между панел€ми;
Ctrl + d Ч закрыть панель (а можно просто набрать exit в терминале).

Ќедостаток Ч непривычным становитс€ скроллинг:
` + PgUp Ч вход в Ђрежим копировани€ї, после чего:
PgUp, PgDown Ч скроллинг;
Esc Ч выход из Ђрежима копировани€ї.


¬ .bash_profile добавить такое:
# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
	. ~/.bash_aliases
fi

—оответственно, .bash_aliases должен выгл€деть так:
alias tmx='tmux -2 attach || tmux -2 new'


# vim:syntax=sh:





¬нутренности .tmux.conf
# tmux config

# prefix as in screen
set -g prefix `
bind ` send-prefix

bind-key ` last-window

# window name
set-option -g set-titles on
set-option -g set-titles-string '[#S-#I-#P] #T :: #W'

# resize pane with arrow keys
unbind Left
unbind Right
unbind Down
unbind Up
bind -r Left resize-pane -L 2
bind -r Right resize-pane -R 2
bind -r Down resize-pane -D 2
bind -r Up resize-pane -U 2

unbind _
unbind |
bind _ split-window -v
bind | split-window -h

# reload config without killing server
bind R source-file ~/.tmux.conf \; display-message "  Config reloaded..."

# displays *
unbind *
bind * list-clients

# lockscreen
unbind x
bind x lock-server

# list windows
unbind w
bind w list-windows

# set window title
unbind T
bind T command-prompt "rename-window %%"

# kill
unbind k
bind k confirm-before "kill-window"

#
source ~/.tmux/tmux-colors-solarized/tmuxcolors-256.conf
source ~/.tmux/tmux-colors-solarized/tmuxcolors-dark.conf


